import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";const root=new URL("../",import.meta.url);test("RC2B.4.2 publica destaques",async()=>{const s=await readFile(new URL("server.mjs",root),"utf8");assert.match(s,/rodrigol-portal-highlights-v1/);assert.match(s,/function publicHighlights/)});test("RC2B.4.2 possui editor",async()=>{const h=await readFile(new URL("public/portal-highlights.html",root),"utf8");const j=await readFile(new URL("public/portal-highlights.js",root),"utf8");assert.match(h,/Destaques do Portal/i);assert.match(h,/global-nav.js/);assert.match(h,/admin-consolidation.css/);assert.match(j,/async function persist/)});