import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';const root=new URL('../',import.meta.url);
test('2.1.2 sincronizacao preserva cadastro editorial preenchido',async()=>{const js=await readFile(new URL('public/data-store.js',root),'utf8');assert.match(js,/current===undefined\|\|current===null\|\|current===''/);assert.match(js,/existing\.crestDataUrl\|\|club\.crestDataUrl/);assert.match(js,/existing\.logoDataUrl\|\|competition\.logoDataUrl/)});
test('2.1.2 portal expoe campeonatos e classificacao no cabecalho',async()=>{const html=await readFile(new URL('public/portal/index.html',root),'utf8');assert.match(html,/href=\"#competitions\" data-nav=\"competitions\">Campeonatos/);assert.match(html,/href=\"#standings\" data-nav=\"standings\">Classificação/);assert.match(html,/view-competitions/);assert.match(html,/loadCompetitions\(\)/)});
