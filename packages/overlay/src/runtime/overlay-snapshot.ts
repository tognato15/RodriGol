export interface ComponentSnapshot{readonly id:string;readonly name:string;readonly type:string;readonly region:string;readonly visible:boolean;readonly box:Readonly<{x:number;y:number;width:number;height:number;zIndex:number}>;readonly data:Readonly<Record<string,unknown>>;readonly transition?:Readonly<{kind:string;durationMs:number}>;}
export interface OverlaySnapshot{readonly sceneId:string;readonly sceneName:string;readonly viewport:Readonly<{width:number;height:number;safeMargin:number}>;readonly theme:Readonly<Record<string,string>>;readonly components:readonly ComponentSnapshot[];readonly revision:number;}
