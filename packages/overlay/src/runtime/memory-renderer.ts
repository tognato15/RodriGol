import type { OverlayRenderer } from "./overlay-renderer.js";import type { OverlaySnapshot } from "./overlay-snapshot.js";export class MemoryOverlayRenderer implements OverlayRenderer{private frames:OverlaySnapshot[]=[];public render(snapshot:OverlaySnapshot):void{this.frames.push(snapshot);}public latest():OverlaySnapshot|undefined{return this.frames.at(-1);}public history():readonly OverlaySnapshot[]{return [...this.frames];}public clear():void{this.frames=[];}}
