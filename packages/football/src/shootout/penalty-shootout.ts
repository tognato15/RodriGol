import { InvariantViolationError } from "@rodrigol/core"; import type { PlayerId } from "../identity/index.js"; import type { TeamSide } from "../match/team-side.js"; import type { PenaltyKick,PenaltyKickOutcome } from "./penalty-kick.js";
export class PenaltyShootout { private readonly kicks:PenaltyKick[]=[]; public register(side:TeamSide,playerId:PlayerId,outcome:PenaltyKickOutcome):PenaltyKick{const expected=this.kicks.length===0?"home":this.kicks.at(-1)?.side==="home"?"away":"home";if(side!==expected)throw new InvariantViolationError("A ordem alternada das cobranças deve ser respeitada.","INVALID_PENALTY_ORDER");const kick={order:this.kicks.length+1,side,playerId,outcome};this.kicks.push(kick);return kick;} public get all():readonly PenaltyKick[]{return [...this.kicks];} public score(side:TeamSide):number{return this.kicks.filter(k=>k.side===side&&k.outcome==="scored").length;} public get winner():TeamSide|undefined{const h=this.score("home"),a=this.score("away");if(this.kicks.length<10||this.kicks.length%2!==0||h===a)return undefined;return h>a?"home":"away";} }
