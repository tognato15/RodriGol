{
  "name": "@rodrigol/editorial",
  "version": "2.0.0-alpha.1",
  "private": true,
  "description": "Motor editorial do RodriGol 2.0 para priorização, publicação e distribuição de conteúdo esportivo.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test",
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('dist',{recursive:true,force:true});\""
  },
  "dependencies": {
    "@rodrigol/core": "2.0.0-alpha.1",
    "@rodrigol/football": "2.0.0-alpha.1"
  }
}

